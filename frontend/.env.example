###############################################
# Frontend Example Environment (.env.example)  #
###############################################
# Copy to frontend/.env and fill in only if using Supabase auth.

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PROJECT_ID=YOUR_PROJECT_REF
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-anon-key

# Enable dev auth bypass (any email/password signs in locally)
VITE_DEV_AUTH=1

# If not using Supabase, you can omit creating a .env file here.
